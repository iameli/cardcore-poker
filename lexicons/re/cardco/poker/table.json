{
  "lexicon": 1,
  "id": "re.cardco.poker.table",
  "defs": {
    "main": {
      "type": "record",
      "description": "Establishes a poker table. First record in any hand. All players must agree on this before the hand begins.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["players", "startingChips", "smallBlind", "createdAt"],
        "properties": {
          "players": {
            "type": "array",
            "description": "Player DIDs in seat order. A freshly-created open room may list only the host (length 1); the roster grows as the host admits joiners.",
            "minLength": 1,
            "maxLength": 10,
            "items": {
              "type": "string",
              "format": "did"
            }
          },
          "startingChips": {
            "type": "integer",
            "minimum": 1,
            "description": "Starting chip count for each player."
          },
          "smallBlind": {
            "type": "integer",
            "minimum": 1,
            "description": "Small blind amount. Big blind is 2x."
          },
          "startedAt": {
            "type": "string",
            "format": "datetime",
            "description": "Set by the host when the roster is locked and the hand begins. Absent while the room is still open for joiners. Once present, players reference this record as the canonical table for the hand."
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime",
            "description": "Bumped periodically by a joining player while they wait to be admitted, so a host that missed the original join-request commit (dropped socket, late subscribe) discovers it from a later one."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
